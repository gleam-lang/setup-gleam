name: Test

on:
  push:
    branches: master
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Install Gleam compiler
        uses: ./
        with:
          gleam-version: 0.4.2
      - name: Run Gleam compiler
        run: gleam version
